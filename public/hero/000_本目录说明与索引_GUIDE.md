# public/hero/｜本目录说明与索引

首屏主视觉素材。

| 文件 | 用途 | 状态 |
|---|---|---|
| `packaging-set.jpg`（2016×1152） | 首页第一屏右侧主视觉：牛皮纸自立袋 + 白色纸盒 + 圆形标签 + 帆布袋 | **AI 生成占位素材**（2026-09-21），待真实拍摄替换 |

替换方式：保持文件名不变直接覆盖，或改名后同步修改 `content/site.ts` 里的 `brand.heroImage.src`。
替换后请把页面上的 `note` 字段一并改成真实素材说明（不要留着「待替换」字样）。
