# content/｜本目录说明与索引

**网站 2 的唯一内容来源。**

| 文件 | 内容 |
|---|---|
| `site.ts` | 品牌与全站文案：`brand`（定位、H1、CTA、内测标注）、`nav`、`formats`（四大形态）、`formatCapabilities`、`packy`（Packy 专区）、`manufacturing`、`platform`（七个环节）、`industries`（行业展厅）、`generalShowroom`（通用展厅）、`categories`（品类网格）、`about`、`footer` |

规则（写死）：

1. 改文案只改本目录，**不在组件里散写**；
2. **不编造能力**：未建成的写「逐步开放 / 内测」，不得写成已具备；
3. 内容口径来自已确认材料（Packy 产品基线、独立站页面方案、专项展厅信息架构、平台蓝图）；
4. 涉及数字、认证、客户、设备等事实时，必须能追溯到已确认记录，否则不写。
